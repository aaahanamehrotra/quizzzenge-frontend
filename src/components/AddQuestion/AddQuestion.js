import React from 'react'

const AddQuestion = (props) => {
    return (
        <div>
            <button onClick={props.startAddingQues}>Add Question</button>
        </div>
    )
}

export default AddQuestion
